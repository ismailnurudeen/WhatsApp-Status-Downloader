<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="MainActivityBaseTheme">
        <!-- Customize your theme here. -->

    </style>

    <style name="MainActivityBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PreviewActivityTheme" parent="MainActivityBaseTheme">
        <!-- Customize your theme here. -->
        <!--<item name="windowBackground">@android:color/black</item>-->
    </style>

    <style name="SplashTheme" parent="MainActivityBaseTheme">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/window_bg</item>
    </style>
</resources>
